# AI 任务中控台产品需求文档（PRD）

> 文档版本：v1.1
> 日期：2026-08-30
> 产品暂定名：AI 任务中控台（AI Agent Control Center）
> 适用平台：macOS、Windows Native、WSL
> 接入工具：Codex、Claude、Cursor、Antigravity IDE

## 1. 文档目标

本文按照问题、用户价值、能力边界、可验证需求、成功指标和发布门槛组织产品要求。它回答五个问题：

1. 用户为什么需要这个产品；
2. 第一版必须解决什么，不解决什么；
3. 如何准确判断 AI 任务状态；
4. 如何安全地向原会话继续发任务；
5. 什么证据足以证明产品可以发布。

技术架构见 [TECHNICAL_IMPLEMENTATION.md](TECHNICAL_IMPLEMENTATION.md)，测试与验收见 [TEST_PLAN.md](TEST_PLAN.md)。

## 2. 产品摘要

用户同时在 Codex、Claude、Cursor 和 Antigravity IDE 中运行多个 AI 任务时，需要频繁切换窗口确认进度，容易漏掉失败、授权请求和已完成结果，也难以在离开电脑后继续下发任务。

本产品在每台电脑安装一个本机节点，通过厂商 Hook、会话事件、CLI、SDK 或 App Server 采集任务状态，统一展示并通过飞书通知用户；对于已登记且可控的会话，用户可以排队下一条任务、恢复历史会话，或在工具支持时向执行中的任务追加信息。

产品不是单纯的坐标点击 RPA。状态判断必须基于确定性事件与可核验证据，GUI 自动化只作为没有正式接口时的降级能力。

## 3. 问题定义

### 3.1 核心问题

- AI 任务分散在多个工具和多台设备，用户没有统一任务视图；
- `Stop`、`idle`、窗口关闭并不等于业务任务成功，现有通知容易误报；
- AI 工具可能等待授权、选择、登录或凭据，如果不及时处理会长期阻塞；
- 用户离开电脑后无法方便地知道任务结果或继续下发下一步；
- 不同工具的会话 ID、事件格式和继续方式不同，无法直接共用一套控制逻辑；
- 纯 UI 自动化容易被窗口布局、弹窗、升级和分辨率变化破坏。

### 3.2 已有验证

macOS 已完成一次四工具真实投递 PoC：Codex、Claude、Cursor、Antigravity IDE 均成功收到相同问题并返回终态。该结果证明“跨 IDE 触发与回读”可行，但尚未证明 Windows、长期运行、故障恢复和所有任务状态均可用。

### 3.3 用户价值

- 一处查看所有 AI 任务的真实状态；
- 失败、等待人工时立即收到手机通知；
- 成功任务自动汇总，减少窗口切换和通知轰炸；
- 在明确能力边界内继续原会话，不丢失上下文；
- 每个结论都能追溯到事件、测试、产物或会话入口。

## 4. 目标与非目标

### 4.1 产品目标

| 编号 | 目标 | 量化标准 |
|---|---|---|
| G-01 | 统一采集四个工具的任务事件 | 支持 Codex、Claude、Cursor、Antigravity 四个适配器 |
| G-02 | 准确区分停止与完成 | P0 验收中“错误宣称成功”数量为 0 |
| G-03 | 及时发现阻塞 | `WAITING_USER`、`FAILED` 的通知 P95 小于 30 秒 |
| G-04 | 支持继续任务 | 四工具均支持 `SEND_NEXT`；托管会话支持 `RESUME_AND_SEND` |
| G-05 | 支持 macOS 与 Windows | macOS、Windows Native、WSL 均通过目标测试矩阵 |
| G-06 | 保护隐私与凭据 | 日志和通知中不出现令牌、密钥、完整 Prompt 或非必要个人信息 |
| G-07 | 监控故障不影响 AI 工具 | Collector 离线时 Hook 在超时内 fail-open |
| G-08 | 跨网络管理个人多台电脑 | 节点仅以出站 443/WSS 接入 ctyun，PC/手机可查看状态并安全续派任务 |

### 4.2 非目标

- 不替代 Codex、Claude、Cursor 或 Antigravity 的原生客户端；
- 不承诺控制任意已结束、未登记的手工 GUI 会话；
- 不修改厂商内部数据库或伪造厂商事件；
- 不以模型自然语言自述作为唯一完成证据；
- MVP 不执行跨设备文件同步、代码合并或自动发布；
- MVP 不提供团队计费、模型成本结算或厂商账单审计；
- MVP 不追求完全无人值守的 GUI 自动化，真正无人值守任务应使用 CLI、SDK 或 Cloud Agent。

## 5. 目标用户

| 用户 | 典型任务 | 主要诉求 |
|---|---|---|
| 多 AI 工具开发者 | 同时让多个 Agent 编码、测试、调研 | 少切窗口、不错过授权、能继续任务 |
| 技术负责人 | 跟踪多个项目和设备上的 AI 工作 | 汇总状态、证据、失败原因和耗时 |
| 自动化管理员 | 安装节点、配置 Hook、维护通知 | 安全、可升级、可诊断、可回滚 |
| 小团队管理者 | 查看团队托管任务和异常 | 最小权限、审计、通知降噪 |

## 6. 产品原则

1. **事件优先，UI 兜底**：Hook、App Server、CLI、SDK、Cloud API 优先于截图和坐标点击。
2. **停止不等于成功**：必须同时存在终止事件、结构化结果和必要证据，才可标记 `SUCCEEDED`。
3. **能力透明**：界面只展示当前会话真正支持的操作，不制造“已继续”假象。
4. **本地优先**：完整 Prompt、原始 transcript 和代码画面默认留在设备端。
5. **安全失败**：监控不可用时不得阻断 AI 工具；控制命令不可用时必须明确失败，不能假装已执行。
6. **人工可接管**：权限、支付、发布、删除和敏感数据操作继续遵守原 AI 工具的人工审批。
7. **确定性判定，AI 只做摘要**：状态机由规则驱动，模型只负责把确定结果写成易读中文。

## 7. 核心概念

### 7.1 会话控制模式

| 模式 | 定义 | 状态采集 | 继续任务能力 |
|---|---|---|---|
| `OBSERVED` | 用户在 IDE 中手工创建、产品只观察的会话 | 高 | 只保证 Stop 前排队；结束后因工具而异 |
| `MANAGED` | 产品通过 App Server、CLI、SDK 或 Cloud API 创建或正式登记的会话 | 高 | 可恢复、排队、继续并审计 |

### 7.2 任务状态

| 状态 | 定义 | 是否终态 |
|---|---|---:|
| `DISCOVERED` | 已发现会话，但尚未确认有活动回合 | 否 |
| `QUEUED` | 命令已排队，尚未被 AI 工具接受 | 否 |
| `RUNNING` | 已有明确开始事件，执行中 | 否 |
| `WAITING_USER` | 等待授权、输入、登录、选择或人工操作 | 否 |
| `SUCCEEDED` | 终止事件、结构化成功结果和必要证据均满足 | 是 |
| `FAILED` | 明确失败事件、异常、非零退出或验证失败 | 是 |
| `CANCELLED` | 用户取消、进程中断或会话被终止 | 是 |
| `UNKNOWN` | 只有 idle、Stop、窗口关闭、超时或不可解析结果 | 是，可重新对账 |

允许的核心状态迁移：

```text
DISCOVERED -> QUEUED -> RUNNING -> SUCCEEDED
                              -> FAILED
                              -> CANCELLED
                              -> WAITING_USER -> RUNNING
                              -> UNKNOWN -> RUNNING | FAILED | SUCCEEDED
```

### 7.3 继续任务命令

| 命令 | 语义 | MVP |
|---|---|---:|
| `SEND_NEXT` | 当前回合结束后，把命令作为下一条用户消息执行 | 是 |
| `RESUME_AND_SEND` | 恢复指定历史会话并发送新任务 | 托管会话是；观察会话逐工具开放 |
| `STEER_ACTIVE` | 向正在执行的回合追加信息 | P1；P0 不对外宣称此能力 |
| `OPEN_AND_PREFILL` | 打开工具/工作区并复制任务，由用户粘贴确认 | 是，作为安全的 GUI 降级能力；不模拟粘贴或回车 |

## 8. 产品范围与优先级

### 8.1 P0：可日常使用的 MVP

- macOS、Windows Native 本机节点安装与健康检查；
- Codex、Claude、Cursor、Antigravity 本机 Hook 接入；
- 统一任务列表、任务详情、事件时间线和设备状态；
- `RUNNING`、`WAITING_USER`、`SUCCEEDED`、`FAILED`、`CANCELLED`、`UNKNOWN` 状态；
- 飞书即时异常通知与成功汇总；
- `SEND_NEXT` 命令队列；
- 托管会话的基础 `RESUME_AND_SEND`；
- 本机 SQLite、离线缓存、幂等去重、日志脱敏；
- 安装、升级、卸载与恢复诊断；
- Windows Native 与 WSL 被识别为不同逻辑设备。

### 8.2 P1：稳定版

- 中央控制台、多设备实时同步和远程命令；
- Codex App Server 的 `thread/resume`、`turn/start`、`turn/steer`；
- Claude CLI/SDK 托管会话恢复；
- Cursor Cloud Agents v1 适配与忙碌重试；
- Antigravity 常驻 `stream-json` 会话；
- 飞书交互卡片中的“打开任务”“排队下一步”“标记已处理”；
- 团队 RBAC、设备撤销、审计导出；
- 自动更新、灰度发布和回滚。

其中 v0.2 Personal Sync 已交付单用户中央控制台、PostgreSQL、一次性配对、设备撤销、WSS、离线补传、加密远程命令和响应式 Web 管理；厂商深度托管、飞书交互卡片、团队 RBAC、自动更新仍属于后续迭代，不计入 v0.2 完成范围。

### 8.3 P2：增强版

- 多团队、多租户和策略中心；
- 自定义适配器 SDK；
- 任务模板、条件编排和跨 Agent 工作流；
- 成本与模型使用趋势，但不代替厂商账单；
- 经验证的 UI 自动化模板库；
- 可选的企业 SSO、SIEM、数据保留策略与合规接口。

## 9. 信息架构与主要页面

### 9.1 总览

显示以下内容：

- 运行中、等待人工、失败、状态不明的数量；
- 最近 24 小时完成率和 P95 耗时；
- 每台设备在线状态和最后心跳；
- 四个工具的接入健康度；
- 需要用户处理的任务优先列表。

### 9.2 任务列表

支持按工具、设备、项目、状态、控制模式、时间筛选。每行显示：

- 工具图标和任务标题；
- 当前状态与状态可信度；
- 设备、工作区和会话模式；
- 开始时间、耗时、最后事件；
- 可执行操作及能力说明。

### 9.3 任务详情

- 任务摘要和当前状态；
- 事件时间线；
- 完成判定依据；
- 脱敏后的证据摘要；
- 待处理权限或人工问题；
- 命令队列与投递结果；
- 打开原工具或恢复会话的入口；
- 审计记录和关联通知。

### 9.4 新任务/继续任务

用户选择设备、工具、工作区、会话和控制命令。发送前必须展示：

- 目标会话是否为 `MANAGED`；
- 预计执行方式；
- 是否会等当前回合结束；
- 命令过期时间；
- 需要原工具继续确认的权限；
- 当前会话不支持时的降级方式。

### 9.5 接入与诊断

- 节点版本、系统版本、在线状态；
- 各工具安装位置、版本、Hook 配置和最近一次事件；
- IPC、SQLite、远程连接和飞书状态；
- 一键运行只读自检；
- 导出脱敏诊断包；
- 修复建议不得自动覆盖用户已有 Hook。

## 10. 功能需求

### 10.1 设备与接入

| ID | 需求 | 优先级 | 验收摘要 |
|---|---|---:|---|
| FR-DEV-001 | 节点生成稳定设备 ID 并识别 OS、用户会话和架构 | P0 | 重启后设备 ID 不变，重装可撤销重建 |
| FR-DEV-002 | 自动发现四个工具的安装和运行状态 | P0 | 未安装、已安装未运行、运行中三态准确 |
| FR-DEV-003 | 安装器合并 Hook 配置，不覆盖原配置 | P0 | 带既有 Hook 的安装/升级测试通过 |
| FR-DEV-004 | 每 30 秒发送心跳，离线后本地缓存事件 | P0 | 网络恢复后补发且不重复 |
| FR-DEV-005 | WSL 与 Windows Native 分配不同设备 ID | P0 | 同名会话不会被合并 |

### 10.2 事件采集与状态判定

| ID | 需求 | 优先级 | 验收摘要 |
|---|---|---:|---|
| FR-EVT-001 | 采集开始、停止、失败、取消、等待人工和会话结束事件 | P0 | 四工具目标事件均有契约测试 |
| FR-EVT-002 | 统一事件必须包含来源、设备、会话、回合、类型和时间 | P0 | 缺关键字段进入隔离队列，不误更新状态 |
| FR-EVT-003 | 重复与乱序事件必须幂等处理 | P0 | 1,000 次重复/乱序注入无重复状态和通知 |
| FR-EVT-004 | `Stop`、`idle`、窗口关闭不能单独产生 `SUCCEEDED` | P0 | 负向测试全部通过 |
| FR-EVT-005 | 超时或不可解析结果进入 `UNKNOWN` | P0 | 不得被归类为失败或成功 |
| FR-EVT-006 | 每个终态显示判定证据和可信度 | P0 | 用户可查看事件来源和证据摘要 |

### 10.3 任务视图

| ID | 需求 | 优先级 | 验收摘要 |
|---|---|---:|---|
| FR-TASK-001 | 提供跨工具、跨设备统一任务列表 | P0 | 10,000 条任务筛选 P95 小于 1 秒 |
| FR-TASK-002 | 任务详情展示完整状态时间线 | P0 | 事件顺序按发生时间，可显示晚到事件 |
| FR-TASK-003 | 展示工具真实能力与控制模式 | P0 | 不支持的按钮隐藏或禁用并说明原因 |
| FR-TASK-004 | 支持返回原 IDE/会话 | P0 | 能定位时一键打开，不能定位时复制任务 ID |

### 10.4 继续任务与命令队列

| ID | 需求 | 优先级 | 验收摘要 |
|---|---|---:|---|
| FR-CMD-001 | `SEND_NEXT` 在 Stop Hook 时最多投递一次 | P0 | 重试、崩溃和重复 Hook 下均不重复执行 |
| FR-CMD-002 | 命令包含 UUID、TTL、创建人和目标会话 | P0 | 过期命令绝不执行，审计可追踪 |
| FR-CMD-003 | 同一会话必须持有控制租约才能投递 | P0 | 双控制器并发时只有一个成功 |
| FR-CMD-004 | `RESUME_AND_SEND` 只对明确支持的会话开放 | P0 | 观察会话不能显示虚假成功 |
| FR-CMD-005 | Codex 支持 `STEER_ACTIVE` | P1 | expected turn 不匹配时明确失败，不新建回合 |
| FR-CMD-006 | 忙碌会话进入队列并退避重试 | P0 | 不丢任务，不无界重试 |
| FR-CMD-007 | 自动继续设置次数上限和全局熔断 | P0 | 默认最多 5 次，触发后通知用户 |

### 10.5 通知与汇总

| ID | 需求 | 优先级 | 验收摘要 |
|---|---|---:|---|
| FR-NTF-001 | `FAILED`、`WAITING_USER` 立即通知 | P0 | P95 小于 30 秒 |
| FR-NTF-002 | `SUCCEEDED` 默认按 5 分钟窗口汇总，允许用户配置 | P0 | 同窗口不重复轰炸 |
| FR-NTF-003 | 状态不变不重复通知 | P0 | 重复事件不会产生重复消息 |
| FR-NTF-004 | 通知包含工具、结论、耗时、下一步和入口 | P0 | 字段缺失时明确显示未知，不编造 |
| FR-NTF-005 | 飞书失败进入 Outbox 重试 | P0 | 网络恢复后只发送一次 |
| FR-NTF-006 | 用户可按工具、状态和时间段静音 | P1 | 失败与安全告警不可被普通静音永久关闭 |

### 10.6 证据、隐私与审计

| ID | 需求 | 优先级 | 验收摘要 |
|---|---|---:|---|
| FR-SEC-001 | 默认不上传完整 Prompt、代码、transcript 和截图 | P0 | 网络抓包与数据库检查均无原文 |
| FR-SEC-002 | 密钥存入 macOS Keychain 或 Windows Credential Manager | P0 | 配置文件、日志和崩溃包中无明文 |
| FR-SEC-003 | 日志执行字段级脱敏和长度限制 | P0 | 预设敏感样本全部被遮罩 |
| FR-SEC-004 | 所有远程命令写入不可抵赖审计记录 | P0 | 可查创建、领取、投递、结果和操作者 |
| FR-SEC-005 | 设备可撤销，撤销后不能再同步或收命令 | P1 | 证书/令牌立即失效 |
| FR-SEC-006 | 诊断包默认脱敏并需用户主动导出 | P0 | 不包含密钥、完整 Prompt 和源码 |

## 11. 核心用户流程

### 11.1 首次安装

```text
下载安装 -> 本机自检 -> 选择接入工具 -> 预览 Hook 变更
-> 用户确认 -> 合并配置 -> 触发四工具测试事件
-> 配置飞书 -> 查看首个统一任务
```

### 11.2 异常任务处理

```text
收到 WAITING_USER/FAILED 通知 -> 打开任务详情 -> 查看确定性事件与原因
-> 打开原会话 -> 完成人工审批或修复 -> 状态回到 RUNNING
-> 终态验证 -> 通知关闭
```

### 11.3 排队下一任务

```text
选择 RUNNING 会话 -> 输入下一步 -> 显示 SEND_NEXT 与 TTL
-> 创建命令 -> 当前回合 Stop -> Hook 原子领取命令
-> 厂商确认接受 -> 标记 DELIVERED -> 新回合 RUNNING
```

### 11.4 恢复历史会话

```text
选择已结束 MANAGED 会话 -> 校验设备在线、工作区存在、无活跃租约
-> RESUME_AND_SEND -> 厂商返回接受结果 -> 新回合开始
-> 若能力缺失则降级 OPEN_AND_PREFILL，等待用户确认
```

## 12. 完成判定与证据策略

任务只有满足以下条件才可进入 `SUCCEEDED`：

1. 收到工具的终止事件；
2. 终止原因不是错误、取消、超时或等待输入；
3. 任务输出符合结构化结果协议，或适配器能确定映射为成功；
4. 对声明存在验证的任务，证据实际存在且验证通过；
5. 没有尚未处理的权限请求、后台命令或失败子任务。

建议托管任务返回：

```json
{
  "outcome": "succeeded | failed | waiting_user | cancelled",
  "summary": "完成了什么",
  "evidence": [
    {"type": "test | artifact | link | commit | log", "ref": "脱敏引用"}
  ],
  "next_action": "没有则为空",
  "confidence": "high | medium | low"
}
```

模型生成的总结不可改变状态机结论。如果模型总结与确定性事件冲突，界面显示事件结论并标记“摘要冲突”。

## 13. 通知内容规范

即时通知示例字段：

| 字段 | 要求 |
|---|---|
| 标题 | `[等待人工] Cursor · 招生模块测试` |
| 状态 | 明确状态与发生时间 |
| 原因 | 来自确定性事件的脱敏原因 |
| 已完成 | 已验证的步骤，不复述完整 Prompt |
| 需要你做 | 一个明确动作 |
| 入口 | 打开任务或复制会话 ID |
| 操作 | 打开、排队下一步、标记已处理；按能力显示 |

成功汇总按工具和项目分组，显示成功数、失败数、状态不明数、总耗时和需要处理的下一步。

## 14. 成功指标

### 14.1 产品指标

- 每周活跃设备数；
- 每周被统一采集的任务数；
- 用户从通知到处理 `WAITING_USER` 的中位时间；
- 通过产品继续任务的使用率；
- 每用户每日减少的 IDE 切换次数；
- 成功汇总打开率与即时告警处理率。

### 14.2 质量指标

| 指标 | MVP 门槛 | 稳定版目标 |
|---|---:|---:|
| 终态事件漏报率 | < 1% | < 0.2% |
| 重复通知率 | < 1% | < 0.1% |
| 错误宣称成功 | 0 | 0 |
| 本地事件入库 P95 | < 3 秒 | < 1 秒 |
| 即时告警 P95 | < 30 秒 | < 10 秒 |
| 命令重复执行 | 0/1,000 | 0/10,000 |
| Hook 对原工具阻断 | 0 | 0 |
| 节点 72 小时稳定运行 | 通过 | 通过 |

## 15. 发布阶段

| 阶段 | 范围 | 退出条件 |
|---|---|---|
| POC | macOS 四工具采集与单次投递 | 已完成 4/4 基础验证 |
| Alpha | macOS Hook、状态机、SQLite、飞书、SEND_NEXT | P0 单元/契约测试通过，内部使用 3 天 |
| Beta | Windows Native、WSL、中央同步、安装升级 | 全平台 E2E 通过，连续运行 7 天 |
| GA | 权限、审计、自动更新、恢复与支持流程 | 发布门禁全部通过，无 P0/P1 缺陷 |

## 16. 依赖与风险

| 风险 | 影响 | 产品处理 |
|---|---|---|
| 厂商 Hook/API 变化 | 状态或控制中断 | 适配器版本探测、契约夹具、功能开关、快速回滚 |
| Cursor 本机任意历史会话缺少公开恢复 API | 无法稳定后台唤醒 | 仅承诺托管会话；观察会话使用打开并预填 |
| Cloud API 仍是 Beta | 接口和限额变化 | 与本机路径解耦，Beta 能力默认关闭 |
| GUI 更新改变布局 | UI 兜底失败 | 健康检测、坐标版本化、默认禁用无人值守 |
| 设备休眠/退出登录 | 本机任务中断 | 明确离线，缓存事件；无人值守改用 CLI/Cloud |
| 多控制器抢同一会话 | 重复执行 | 会话租约、幂等命令、CAS 状态更新 |
| Prompt 或源码泄露 | 高安全风险 | 本地优先、字段脱敏、最少上传、短期保留 |
| 自动继续死循环 | 资源浪费和错误扩大 | 次数上限、TTL、预算、熔断和人工通知 |

## 17. 待确认产品决策

以下项目不阻塞 Alpha，但进入 Beta 前需要确认：

1. 产品是单用户本地工具，还是从第一版支持团队多租户；
2. 中央服务部署在用户自有服务器、公司内网还是托管云；
3. 飞书之外是否需要 Slack、Teams、邮件或手机推送；
4. 完整 Prompt 是否允许在设备端加密保存，以及默认保留周期；
5. 哪些高风险命令必须二次确认，哪些只允许打开并预填；
6. 是否支持自动创建托管 CLI/Cloud 会话，还是只接管用户已有会话；
7. 团队管理员能否查看成员任务摘要和工作区名称。

## 18. 官方能力依据

- Codex Hook 能覆盖 Stop、SessionEnd 等生命周期事件，并允许 Stop 阶段要求继续执行：[OpenAI Codex Hooks](https://learn.chatgpt.com/docs/hooks)
- Codex App Server 提供 `thread/resume`、`turn/start` 和 `turn/steer` 等会话控制接口：[OpenAI Codex App Server](https://learn.chatgpt.com/docs/app-server)
- Claude Stop Hook 可以阻止停止并继续会话，SessionStart 在恢复时再次触发：[Claude Code Hooks](https://code.claude.com/docs/en/hooks)
- Claude 会话可通过 `--continue` 或 `--resume` 恢复：[Claude Code Sessions](https://code.claude.com/docs/en/sessions)
- Cursor stop Hook 支持 `followup_message`，并提供自动继续次数限制：[Cursor Hooks](https://cursor.com/docs/hooks)
- Cursor Cloud Agents v1 是公开 Beta；v1 Webhook 尚未开放：[Cursor Cloud Agents API](https://cursor.com/docs/cloud-agent/api/endpoints)
- Antigravity Stop Hook 提供 `conversationId`、`terminationReason`、`fullyIdle` 和继续决策：[Antigravity Hooks](https://antigravity.google/docs/hooks)
- Antigravity CLI 支持 `stream-json`、`--continue` 和按会话 ID 恢复：[Antigravity Headless Mode](https://antigravity.google/docs/cli/headless/)

## 19. 关联文档

- [AI_TASK_RPA_FEASIBILITY.md](AI_TASK_RPA_FEASIBILITY.md)：技术可行性和真实 PoC
- [TECHNICAL_IMPLEMENTATION.md](TECHNICAL_IMPLEMENTATION.md)：技术选型、架构、接口和实施步骤
- [TEST_PLAN.md](TEST_PLAN.md)：自动化测试、真机验证、故障注入和发布门禁
- [runs/20260829-model-probe/summary.md](runs/20260829-model-probe/summary.md)：macOS 四工具实测摘要
