# 安全策略

## 支持范围

| 版本 | 安全更新 |
| --- | --- |
| `main` | 支持 |
| 最新发布版本 | 支持 |
| 更早版本 | 不保证 |

项目仍处于早期阶段。安全修复会优先进入 `main`，必要时回补到最新发布版本。

## 私密报告漏洞

请不要为安全漏洞创建公开 Issue，也不要提交包含真实密钥、提示词、会话内容或企业源码的 PoC。

请打开仓库的 **Security** 页面，选择 **Report a vulnerability**，并提供：

- 受影响版本或提交
- 受影响平台和 AI 工具
- 可复现的最小步骤
- 预期影响和攻击前提
- 已脱敏的日志或样例
- 建议修复（如有）

维护者会尽快确认报告、评估影响并协调披露时间。修复发布前，请不要公开漏洞细节。

## 特别关注范围

- 本地 API 越权或非回环地址暴露
- Hook 配置注入、命令注入或不安全覆盖
- Keychain / Credential Manager、SQLite 或日志中的敏感信息泄露
- 飞书 Webhook、AI 工具会话令牌或提示词泄露
- 任务状态伪造、错误续跑或跨任务命令投递
- 更新、安装和签名链路的供应链风险

仅包含社会工程、需要已完全控制本机、或不产生额外安全影响的报告，可能不会被视为漏洞，但仍欢迎提交改进建议。
