# AI 任务中控台测试与验证计划

> 版本：v1.1
> 日期：2026-08-30
> 对应 PRD：[PRODUCT_PRD.md](PRODUCT_PRD.md)
> 对应技术方案：[TECHNICAL_IMPLEMENTATION.md](TECHNICAL_IMPLEMENTATION.md)

## 1. 测试目标

测试重点不是证明“能收到一次消息”，而是证明系统在多工具、多操作系统、重复/乱序事件、断网、崩溃和权限阻塞下：

1. 不会把 Stop、idle 或窗口关闭误报为成功；
2. 不会丢失、重复执行或错误路由后续任务；
3. 监控故障不会阻断原 AI 工具；
4. 故障和等待人工能及时、去重地通知；
5. Prompt、源码、凭据和个人数据不会被日志/通知意外带出；
6. macOS、Windows Native 和 WSL 的真实安装与会话流程均可复现。

## 2. 测试范围

### 2.1 范围内

- Codex、Claude、Cursor、Antigravity 的 Hook/CLI/API 适配器；
- 事件校验、规范化、幂等、乱序处理和状态机；
- 本地 SQLite、Outbox、离线缓存、恢复和数据迁移；
- `SEND_NEXT`、`RESUME_AND_SEND`、`STEER_ACTIVE`、`OPEN_AND_PREFILL`；
- 本地与中央控制台、设备同步和权限；
- 飞书即时通知、聚合、失败重试和卡片回调；
- macOS、Windows Native、WSL 安装升级与长期运行；
- 安全、性能、故障恢复和可观测性。

### 2.2 范围外

- 验证厂商模型回答内容的事实正确性；
- 替代厂商自身对代码执行、文件写入、网络和敏感操作的权限控制；
- 对所有历史版本或非官方修改版 IDE 提供兼容保证；
- 对未登记的任意 GUI 会话承诺可恢复；
- 正式商业环境的真实支付、发布或删除操作。此类 UAT 需单独授权和清理方案。

## 3. 质量风险排序

| 风险 | 影响 | 概率 | 测试优先级 |
|---|---:|---:|---:|
| Stop/idle 被误判成功 | 极高 | 高 | P0 |
| 重试导致下一任务重复执行 | 极高 | 中 | P0 |
| 命令投递到错误会话/设备 | 极高 | 中 | P0 |
| Hook 故障阻塞 AI 工具 | 高 | 中 | P0 |
| Prompt/令牌进入日志或飞书 | 极高 | 中 | P0 |
| 厂商升级导致事件契约变化 | 高 | 高 | P0 |
| 断网/休眠后事件丢失 | 高 | 高 | P0 |
| UI 自动化误点击 | 高 | 高 | P0（降级功能） |
| 通知重复或过晚 | 中 | 中 | P1 |
| 大量历史任务查询变慢 | 中 | 中 | P1 |

## 4. 环境矩阵

### 4.1 操作系统

| 环境 | 架构 | 用途 | 发布要求 |
|---|---|---|---|
| macOS 当前主版本 | Apple Silicon | 主要开发和真实 IDE E2E | 必测 |
| macOS 当前主版本 - 1 | Apple Silicon | 向后兼容 | 必测 |
| macOS 支持范围内 | Intel | 二进制/安装兼容 | 有设备则必测，否则列为限制 |
| Windows 11 当前版本 | x64 | Native IDE E2E | 必测 |
| Windows 11 当前版本 | ARM64 | 安装/运行兼容 | 若承诺支持则必测 |
| WSL2 Ubuntu LTS | x64/ARM64 随宿主 | CLI 与路径互操作 | 必测 |

不把 Wine、虚拟化 macOS 或 Windows Server 当成桌面 IDE 的正式替代环境。

### 4.2 工具版本

- 每个工具测试“当前稳定版”和“仍在支持范围的前一稳定版”；
- Preview/Beta 版只做兼容观察，不计入 GA 通过率；
- 每次厂商升级先运行 Fixture 契约测试，再在隔离项目执行真实 Smoke Test；
- Cursor Cloud v1 以 Public Beta 对待，不把未开放的 v1 Webhook 放入发布前置条件。

### 4.3 网络与账户

- 正常网络、高延迟（200/500/1,500 ms）、1%/5% 丢包、完全离线、代理、证书错误；
- 飞书正常、限流、5xx、超时、签名错误、用户无权限；
- 已登录、登录过期、无权限、工具未安装、工具进程崩溃；
- 系统睡眠、唤醒、注销、快速用户切换、时钟偏移和磁盘空间不足。

## 5. 测试层级与自动化比例

| 层级 | 目标 | 自动化目标 | 运行频率 |
|---|---|---:|---|
| 单元测试 | 状态机、脱敏、重试、路径、配置 | 100% 自动 | 每次提交 |
| 属性/模糊测试 | 任意事件序列下不误报、不 panic | 100% 自动 | 每次提交/夜间扩展 |
| 契约测试 | 四厂商输入/输出协议 | 100% 自动 | 每次提交 + 每日 |
| 组件测试 | SQLite、Outbox、IPC、命令租约 | ≥95% 自动 | 每次提交 |
| 集成测试 | Node/Server/Feishu Mock | ≥90% 自动 | PR/主分支 |
| API E2E | 多设备、权限、通知、恢复 | ≥90% 自动 | 主分支 |
| 真实 IDE E2E | 四工具真实运行 | 核心流程自动/半自动 | 每日或发布前 |
| 安装/升级 | 签名包、Hook 合并、回滚 | 自动 + 人工检查 | 每个 RC |
| 探索性/UAT | 可用性和异常恢复 | 人工 | 每个 RC |

覆盖率门槛：状态机、命令队列、脱敏模块分支覆盖率不低于 95%；Rust 核心总体行覆盖率不低于 85%；前端关键业务逻辑不低于 80%。覆盖率不能替代 P0 场景通过。

## 6. 测试数据与夹具

每个适配器维护脱敏 Golden Fixtures：

```text
testdata/providers/{provider}/{version}/
  start.json
  stop-success.json
  stop-without-result.json
  waiting-user.json
  failed.json
  cancelled.json
  malformed.json
  oversized.json
  duplicate.json
  expected-normalized.json
```

要求：

- Fixture 来源于真实事件结构，但必须替换用户路径、仓库名、Prompt、回复、令牌和会话 ID；
- 原始结构与预期统一事件成对保存；
- 发现未知字段应兼容保留，缺少关键字段进入隔离队列；
- 每个正式支持的工具版本至少有一组 Fixture；
- 真机测试产生的原始截图、日志和 transcript 默认不提交仓库。

## 7. 核心功能测试用例

### 7.1 事件与状态机

| ID | 场景 | 操作/输入 | 预期结果 |
|---|---|---|---|
| EVT-001 | 正常开始 | start 事件 | `DISCOVERED/QUEUED -> RUNNING`，只产生一次时间线记录 |
| EVT-002 | Stop 无结果 | 仅 Stop/idle | `UNKNOWN`，不得为 `SUCCEEDED` |
| EVT-003 | 一般问答成功 | Stop + 结构化成功 | 达到 E2，标记 `SUCCEEDED` |
| EVT-004 | 编码任务缺测试 | 模型声称完成但无所需 E3 | `UNKNOWN` 或“待验证”，不得成功 |
| EVT-005 | 工具执行失败 | 非零退出/错误事件 | `FAILED`，保存脱敏原因 |
| EVT-006 | 等待授权 | 权限/登录/选择事件 | `WAITING_USER`，恢复后回到 `RUNNING` |
| EVT-007 | 用户取消 | cancel/interrupt | `CANCELLED`，不自动重试任务 |
| EVT-008 | 事件重复 | 同一事件重复 1,000 次 | 一条事件、一条状态迁移、一次通知 |
| EVT-009 | 事件乱序 | stop 先到、start 后到 | 最终状态正确，时间线标记晚到事件 |
| EVT-010 | 晚到错误 | 已有成功候选后收到同回合错误 | 依据优先级更正为失败并发更正通知 |
| EVT-011 | 未知 Schema | 新版本未知字段 | 不崩溃；保留字段；关键字段缺失则隔离 |
| EVT-012 | 超大/恶意载荷 | 超限 JSON、深层嵌套、非法 UTF-8 | 有界拒绝，不影响 Hook 和节点 |
| EVT-013 | 多会话交错 | 100 个会话随机事件 | 不串会话、不串项目、不串设备 |
| EVT-014 | 重放重建 | 删除派生快照后重放事件 | 终态与原快照一致 |

### 7.2 命令投递

| ID | 场景 | 操作/输入 | 预期结果 |
|---|---|---|---|
| CMD-001 | Stop 时有一条队列 | 创建 `SEND_NEXT` 后触发 Stop | 只返回/投递一条，状态可追踪 |
| CMD-002 | 重复 Stop | 相同 Stop 并发 20 次 | 后续任务最多执行一次 |
| CMD-003 | FIFO | 连续排队 3 条 | 按顺序逐回合执行，不能合并 |
| CMD-004 | TTL 过期 | 工具长期未停止 | 命令变为 `EXPIRED`，不得后补执行 |
| CMD-005 | 节点崩溃前 | 领取 lease 后、投递前崩溃 | lease 到期安全重领 |
| CMD-006 | 节点崩溃窗口 | 已调用厂商、ACK 前崩溃 | 标记 `UNKNOWN_DELIVERY`，不静默重投 |
| CMD-007 | 多节点竞争 | 两节点尝试领取同命令 | 只有目标设备可领取且仅一个 lease |
| CMD-008 | 错误目标 | 会话不属于设备/用户 | 拒绝并审计，无厂商调用 |
| CMD-009 | 会话忙碌 | Cursor Cloud 返回 busy | 抖动退避，TTL/次数到达后明确失败 |
| CMD-010 | 活动 Steer 冲突 | Codex `turnId` 已变化 | 不投错回合，降级排队或返回冲突 |
| CMD-011 | 无限跟进保护 | 模型持续要求继续 | 达到回合/时长上限后停止并通知 |
| CMD-012 | 敏感动作 | 后续 Prompt 要求发布/删除/支付 | 交由原工具权限机制，产品不自动批准 |
| CMD-013 | 观察会话不可恢复 | 手工 IDE 会话无可靠 ID | 只显示 `OPEN_AND_PREFILL`，不声称已恢复 |

### 7.3 飞书通知

| ID | 场景 | 预期结果 |
|---|---|---|
| NTF-001 | 失败即时通知 | P95 小于 30 秒，内容脱敏且有安全链接 |
| NTF-002 | 等待人工 | 只通知一次；状态变化或提醒间隔到达才再通知 |
| NTF-003 | 成功聚合 | 聚合窗口内合为一条，数量和任务链接准确 |
| NTF-004 | 重复事件 | 相同状态版本最多一条通知 |
| NTF-005 | 飞书限流/5xx | Outbox 重试，不阻塞事件处理，不重复最终消息 |
| NTF-006 | 永久失败 | 达最大重试后控制台告警并可人工重发 |
| NTF-007 | 卡片回调重放 | 签名、时效和 nonce 校验，重复回调无副作用 |
| NTF-008 | 用户越权 | 无权用户不能查看详情或下发任务，记录审计 |
| NTF-009 | 内容泄漏 | 通知不含密钥、完整 Prompt、完整本地路径或源码 |

### 7.4 多机中央控制与移动端

| ID | 场景 | 预期结果 |
|---|---|---|
| CEN-001 | 一次性配对 | 配对码 15 分钟过期且只可消费一次；数据库只保存令牌哈希 |
| CEN-002 | 跨公网连接 | Mac/Windows 节点只建立出站 HTTPS/WSS 443，不要求开放电脑入站端口 |
| CEN-003 | 设备身份不一致 | Token 对应设备与心跳/事件中的设备 ID 不同，中央端拒绝并留痕 |
| CEN-004 | 设备撤销 | 撤销后当前 WSS 断开，旧令牌不能重连或继续上报 |
| CEN-005 | 断网补传 | 事件先落本机 SQLite；恢复后批量补传，ACK 后才清队列，重复批次不重复派生任务 |
| CEN-006 | 远程命令加密 | PostgreSQL 不出现明文任务内容；节点只能接收结构化 AI 消息，协议中不存在任意 shell 字段 |
| CEN-007 | 命令路由 | 命令只能由匹配设备、工具和会话接收；错误能力或观察会话恢复被拒绝 |
| CEN-008 | 重连竞争 | 同一设备新连接替换旧连接时，旧连接结束不能误删新通道或把设备标成离线 |
| CEN-009 | 移动端 390×844 | 总览、设备、任务详情、续派可操作，无水平滚动，关键按钮触控高度至少 40px |
| CEN-010 | 桌面端 1440×1000 | 在线/离线设备、一次性配对码、任务状态和远程续派入口完整可见 |
| CEN-011 | 手机刷新/重复提交 | 页面刷新不重复创建命令；提交中按钮禁用，服务端仍执行队列上限与幂等校验 |
| CEN-012 | ctyun 服务故障 | RDS/中央服务不可用时本机监控继续运行；恢复后 WSS 指数退避重连并补传 |

## 8. 四工具真机验收

每个工具在 macOS 和 Windows 对下列场景各运行至少一次；支持 WSL 的 CLI 场景在 WSL 再运行一次：

| 场景 | Codex | Claude | Cursor | Antigravity |
|---|---:|---:|---:|---:|
| 普通问答成功（E2） | 必测 | 必测 | 必测 | 必测 |
| 编码 + 自动测试成功（E3） | 必测 | 必测 | 必测 | 必测 |
| 工具命令失败 | 必测 | 必测 | 必测 | 必测 |
| 等待用户授权 | 必测 | 必测 | 必测 | 必测 |
| 用户取消 | 必测 | 必测 | 必测 | 必测 |
| Stop 无完成证据 | 必测 | 必测 | 必测 | 必测 |
| `SEND_NEXT` | 必测 | 必测 | 必测 | 必测 |
| 托管会话恢复 | 必测 | 必测 | 按已验证能力 | 必测 |
| 执行中追加 | 必测 | 降级验证 | 降级验证 | 降级验证 |
| 工具升级后契约回归 | 必测 | 必测 | 必测 | 必测 |

真机 E2E 必须同时保存：工具版本、系统版本、节点版本、测试用例 ID、开始/结束时间、脱敏事件、最终状态、命令 ACK、通知回执和必要证据哈希。只有截图或模型自述不算通过。

## 9. 适配器专项验证

### 9.1 Codex

- Hook Stop/SessionEnd 输入与返回 JSON 契约；
- App Server `thread/resume`、`turn/start` 正常/失败/断线恢复；
- `turn/steer` 的正确 `turnId`、过期 `turnId` 和无活动回合；
- Hook 返回 block/continue 时的循环上限与节点离线 fail-open。

### 9.2 Claude

- Stop、TaskCompleted、StopFailure 的映射；
- `stop_hook_active` 防递归；
- session ID 保存、`--resume` 成功和会话不存在；
- 权限询问、登录失效、CLI 非零退出；
- Hook 多脚本并存时不覆盖用户配置。

### 9.3 Cursor

- `stop` 的 `followup_message`、连续次数和停止条件；
- `sessionEnd`、失败与用户取消的差异；
- 手工 IDE 会话不可恢复时能力按钮正确降级；
- Cloud v1 Run 创建、状态查询/SSE、busy、限流和网络中断；
- 测试明确禁止依赖尚未开放的 v1 Webhook。

### 9.4 Antigravity

- Stop 的 conversation ID、termination reason、`fullyIdle`、model 字段；
- `fullyIdle=true` 但证据不足仍为 `UNKNOWN`；
- `decision: continue` 的单次投递与循环上限；
- `stream-json` 部分行、非法行、中断、persistent stdin；
- conversation ID 恢复成功、过期和无权限。

## 10. 故障与恢复测试

| ID | 故障注入 | 通过标准 |
|---|---|---|
| RES-001 | 节点被强制结束 | IDE 不受阻；重启后恢复未同步事件和 lease |
| RES-002 | SQLite 正在事务时崩溃 | 数据库可恢复，无半条状态/通知事务 |
| RES-003 | 数据库锁/只读/磁盘满 | 有界失败、告警、Hook fail-open |
| RES-004 | 中央服务离线 2 小时 | 本机完整工作；恢复后顺序补发且幂等 |
| RES-005 | 网络反复抖动 | 不形成重连风暴，不重复命令和通知 |
| RES-006 | 电脑睡眠 8 小时 | 唤醒后重建连接、处理过期命令和时钟跳变 |
| RES-007 | 飞书不可用 | 状态照常更新，Outbox 有界重试 |
| RES-008 | 单个适配器崩溃 | 熔断该适配器，其他三个工具正常 |
| RES-009 | 服务端滚动升级 | 已连接节点自动重连，无事件丢失 |
| RES-010 | 节点升级失败 | 回滚上一版本，既有 Hook 仍可用 |

故障测试使用可控制的代理、时钟、磁盘配额和进程终止工具，不在个人真实项目上制造破坏性故障。

## 11. 安全测试

### 11.1 自动化检查

- SAST、依赖漏洞和许可证扫描；
- Secret Scan 覆盖代码、Fixture、构建产物和诊断包；
- `cargo-fuzz`/属性测试覆盖 JSON、路径、Hook 输出和 API 帧；
- API Schema 负向测试、认证/授权矩阵和速率限制；
- 安装包签名、更新清单签名和哈希校验；
- 日志/通知快照测试确保敏感字段脱敏。

### 11.2 攻击场景

| ID | 场景 | 通过标准 |
|---|---|---|
| SEC-001 | Prompt 伪装成“执行管理员命令” | 仅作为文本进入目标 AI，不改变控制面策略 |
| SEC-002 | Hook JSON 注入 shell 元字符 | 无 shell 拼接，无额外进程/命令执行 |
| SEC-003 | 路径穿越/符号链接 | 不能读写允许目录外文件 |
| SEC-004 | 伪造设备/重放命令 | mTLS/签名/nonce/TTL 拒绝并审计 |
| SEC-005 | 横向越权 | 用户 A 不能查看或控制用户 B 的设备会话 |
| SEC-006 | 本地低权限进程访问 IPC | UDS/Named Pipe ACL 拒绝 |
| SEC-007 | 诊断包检查 | 不含令牌、Cookie、源码、完整 Prompt、个人路径 |
| SEC-008 | Web 深链 | 无开放重定向，登录后仍检查任务权限 |
| SEC-009 | 飞书卡片回调伪造 | 验签失败、无状态变化 |
| SEC-010 | GUI 驱动失焦 | 立即停止，不把文本发到非目标窗口 |

GA 前要求无未处理 Critical/High 漏洞；Medium 必须有负责人、期限和风险接受记录。

## 12. 性能与稳定性测试

### 12.1 基准

- 单节点持续 100 events/s，峰值 1,000 events/s，持续 10 分钟；
- 10,000 个任务、100,000 个事件下列表/详情查询；
- 100 台模拟节点同时重连和补发；
- 10,000 条重复/乱序事件注入；
- 飞书 429/5xx 下 10,000 个通知事件的退避和聚合；
- Hook 入口冷/热启动 P50/P95/P99。

### 12.2 稳定性

- macOS 与 Windows 各进行 72 小时 Soak；
- 每小时运行四工具模拟事件、命令、断连和恢复；
- 无崩溃、无内存持续增长、无数据库损坏、无重复命令；
- 内存增长小于基线的 10%，磁盘增长符合保留策略；
- 适配器错误不导致节点整体不可用。

## 13. 安装、升级与卸载

| ID | 场景 | 通过标准 |
|---|---|---|
| INS-001 | 全新安装 | 节点启动、健康通过、四工具检测正确 |
| INS-002 | 已有 Hook | 合并配置且保持原 Hook 顺序/语义，备份可恢复 |
| INS-003 | 重复安装 | 幂等，不产生多个进程或重复 Hook |
| INS-004 | 跨版本升级 | 数据 migration、命令、证据和配置保留 |
| INS-005 | 升级中断 | 自动回滚或可修复，不破坏厂商配置 |
| INS-006 | 卸载保留数据 | 程序和 Hook 移除，历史按选择保留 |
| INS-007 | 完全卸载 | 删除产品数据和凭据，但不删除厂商会话/项目 |
| INS-008 | 无管理员权限 | 支持每用户安装；需管理员时明确提示 |
| INS-009 | 签名/公证 | macOS Gatekeeper、Windows SmartScreen/企业策略验证 |

卸载器只能删除安装清单中明确登记且校验归属的路径，不使用广泛递归删除。

## 14. 前端与可用性验证

- 任务状态不能只用颜色表达；图标、文字和时间均可读；
- 键盘可完成筛选、打开任务、排队和取消；
- macOS VoiceOver、Windows Narrator 核心流程可用；
- 200% 缩放、窄窗口、长中文/英文和超长项目名不遮挡关键操作；
- 所有危险/不可逆动作二次确认；
- 不支持的操作显示原因和建议降级方式；
- 断网、节点离线、厂商未安装和会话不可恢复状态明确；
- 浏览器刷新不导致重复命令。

## 15. CI、夜间测试与发布流水线

### 15.1 每次提交

1. 格式化、lint、类型检查；
2. Rust 单元/组件测试、Clippy 和 Miri/TSan（适用模块）；
3. 前端单元测试；
4. 四适配器 Fixture 契约测试；
5. Secret/SAST/依赖扫描；
6. Linux/Windows/macOS 构建；
7. `git diff --check` 与文档链接检查。

### 15.2 每夜

- 扩展 fuzz/property 测试；
- Node/Server/飞书 Mock E2E；
- 最新稳定工具版本的契约抓取与变化告警；
- 自托管 macOS/Windows Runner 的真实 IDE Smoke；
- 数据迁移、备份恢复和离线补发测试。

### 15.3 Release Candidate

- 全环境矩阵；
- 真实四工具 P0 E2E；
- 安装/升级/回滚；
- 72 小时 Soak；
- 安全复核和权限审计；
- 产品负责人 UAT；
- 签名产物的 SBOM、哈希和测试报告归档。

真实 IDE 自动化 Runner 必须使用隔离测试账户、最小权限测试项目和无生产凭据环境。

## 16. UAT 场景

### UAT-01：跨工具任务汇总

同时在四个工具启动任务。控制台在 5 秒目标内出现四条任务，工具、设备、项目、状态和耗时正确；完成后收到一条聚合飞书消息。

### UAT-02：成功证据门槛

让 Agent 修改一个隔离项目：第一次只声称完成但不运行测试，系统不得标记 E3 成功；补充测试证据后才转为 `SUCCEEDED`。

### UAT-03：等待人工

任务触发权限或登录询问。系统转为 `WAITING_USER` 并即时通知；用户处理后恢复运行，时间线完整且不重复通知。

### UAT-04：跨设备继续任务

在手机飞书打开安全页面，选择 Windows 上一个已结束的托管会话并发送下一步。命令只在目标会话执行一次，结果回到同一任务链。

### UAT-05：离线恢复

中央端断网期间运行并完成多个任务。节点本地可查看；恢复网络后补齐中央状态和聚合通知，无丢失、无重复。

### UAT-06：能力降级透明

选中一个无法稳定恢复的手工 Cursor IDE 会话。页面不得出现“远程恢复并发送”，只能打开并预填，且明确需要用户确认。

### UAT-07：监控故障不阻塞

停止本机节点后在四个工具完成任务。Hook 在超时内 fail-open，原工具正常；节点恢复后能对账或明确显示观测缺口。

## 17. 需求—测试追踪矩阵

| PRD 需求组 | 自动化/专项用例 | 真机/UAT | 发布证据 |
|---|---|---|---|
| `FR-DEV-*` | INS-001～009、RES-001/006/010 | 四工具环境矩阵、UAT-07 | 安装日志、健康检查、Hook 配置差异 |
| `FR-EVT-*` | EVT-001～014、各适配器专项 | UAT-01/02/03/07 | 脱敏事件、状态重放、状态机版本 |
| `FR-TASK-*` | API E2E、前端可用性/性能 | UAT-01/06 | 页面录屏或截图、查询指标、权限结果 |
| `FR-CMD-*` | CMD-001～013、SEC-001/002/004/005 | UAT-04/06 | command ID、lease、厂商 ACK、审计日志 |
| `FR-NTF-*` | NTF-001～009、RES-007 | UAT-01/03/05 | 飞书 message ID、Outbox、延迟指标 |
| `FR-SEC-*` | SEC-001～010、Secret Scan、SAST | UAT-04/06 | 扫描报告、权限矩阵、脱敏诊断包 |

每个 P0 需求必须在测试管理系统或发布报告中链接到至少一个自动化用例和一个适用的真机/UAT 证据。需求变更时，缺失映射即阻止合并；不适用项必须说明理由，不能留空。

## 18. 发布门禁

| 门禁 | Alpha | Beta | GA |
|---|---:|---:|---:|
| P0 自动化测试通过率 | 100% | 100% | 100% |
| P1 自动化测试通过率 | ≥95% | ≥98% | 100% 或已批准例外 |
| 错误宣称成功 | 0 | 0 | 0 |
| 重复命令执行 | 0/1,000 | 0/10,000 | 0/100,000 注入与全量 E2E |
| Critical/High 安全问题 | 0 | 0 | 0 |
| macOS 真机 | 通过 | 通过 | 通过 |
| Windows Native 真机 | 可缺失并标注 | 通过 | 通过 |
| WSL 真机 | 可缺失并标注 | 通过 | 通过 |
| 72 小时 Soak | 不要求 | 通过 | 通过 |
| 安装升级回滚 | 基础 | 通过 | 全支持矩阵通过 |
| 飞书真实闭环 | 单用户 | 多设备 | 权限、重试、审计通过 |

任何一项 P0 失败、任何误报成功、任何未解释的重复命令或高危数据泄漏都阻止发布。测试未执行必须标记 `NOT RUN`，环境不适用标记 `N/A`，不能计为通过。

## 19. 缺陷与证据管理

### 18.1 严重级别

- S0：数据/凭据泄漏、跨用户控制、错误设备执行、破坏性误操作；立即停止测试和发布；
- S1：误报成功、重复命令、Hook 阻塞工具、状态大面积丢失；阻止发布；
- S2：单适配器异常、通知明显延迟、可恢复安装问题；Beta/GA 前修复；
- S3：视觉、文案或低频兼容问题；进入排期。

### 18.2 每轮报告必须包含

- Git commit 与构建产物哈希；
- 系统、工具、节点和服务版本；
- 测试总数及 Passed/Failed/Skipped/NOT RUN/N/A；
- 每个发布门禁结果；
- 失败用例、日志 trace ID、复现步骤和负责人；
- 真实 IDE 与 Mock/Fixture 结果分别统计；
- 隐私审查结论和清理结果；
- 对未验证能力的明确限制。

“构建成功”“HTTP 200”“收到 Hook”“模型回复完成”都不是完整业务闭环。只有预期状态、正确会话、必要证据、通知回执和无重复副作用共同满足，才算对应场景通过。
